# 2026-09-11 发布验收：R2 上传与教学示例

验收日期为香港时间 2026-09-11。已验证入口为 <https://tashan.kh107843099.workers.dev>，连接真实 Supabase Auth / Postgres、私有 R2 和 DeepSeek；不是模拟云提供方。本文记录本轮结果，较早的 QA 报告继续保留，按各自日期理解。

**正式域名 <https://tashan.dev> 已绑定并上线，实际写入、读取和 AI 复测也已通过。** Cloudflare 确认域名指向 `tashan` 的 production 环境，HTTPS 返回 200；域名只读验收 **104 / 104 通过，共 125 个请求**。检查识别到 11 个外部依赖，未主动请求这些外站。

当前部署版本为 `29129f58-62f1-49eb-b4e8-88fa2e40f9ab`；保留 workers.dev 入口，关闭版本预览 URL。

## 接口与文件

| 检查 | 结果 | 已验证范围 |
| --- | --- | --- |
| 真实 Worker 正向流程 | 58 / 58 通过 | 管理员创建成员、新成员无需首次改密即可使用项目与 AI 接口、主动改密及重新登录、角色限制、两版上传与读取、稳定编号和来源、重复上传、公开与撤回 |
| 正式域名业务复测 | 58 / 58 通过 | 在 `tashan.dev` 重跑真实账号、上传下载、版本、所有权、公开与撤回流程 |
| 正式域名示例、安全与 AI | 27 / 27 通过 | 管理员与 `test` 登录、Secure / HttpOnly / SameSite=Strict Cookie、跨域写入拒绝、两示例读取与文件 SHA-256、AI 填齐八项上传资料、登出与重新登录 |
| 真实 R2 负向流程 | 35 / 35 通过 | 超过 10 MiB 在预留前拒绝；伪造 SHA-256 返回 422；错误 PNG 文件头返回 415；失败快照不能完成或公开；账号停用后会话失效 |
| 文件完整性与隔离 | 通过 | 附件及封面下载后 SHA-256 一致；HTML 使用隔离响应；管理员不能直接读取成员私有文件，游客不能读取未发布版本 |
| 原生运行时回归 | 7 / 7 通过 | 本地 workerd 的 10 MiB 附件、5 MiB 封面、固定长度多／少字节、真实校验接口、并发条件写入及重试 |

新上传走原始二进制流，Supabase 保存版本与权限，R2 保存新文件；旧 Supabase Storage 文件不被搬迁或删除。正向、负向和正式域名验收的临时成员已停用、会话已撤销，验收项目已撤回公开；仅保留生成的私有测试快照与审计，未删除原有资料。`test` 账号保持启用，下面两个教学示例继续公开。

前一轮 workers.dev 请求 CPU 样本约为 **4–7 ms**；正式域名上传流程样本为 **2–9 ms**，其中 10 MiB 附件与 5 MiB 封面的 PUT 均为 4 ms，prepare 为 9 ms，commit 为 3 ms。本轮运行观测的 166 个请求样本均正常、无异常，不能推断长期负载或容量保证。Workers 仍按 Free 的 10 ms CPU 边界评估；开通 R2 不代表开通 Workers Paid。未进行持续并发压测或真实浏览器内存测试。

## 两个公开示例

两个项目均已由 `test` 账号上传并公开，重新登录后的读取仍存在；源文件和封面下载哈希与本地文件一致。

| 示例 | 平台项目编号 | 已上传文件 | 当前版本 |
| --- | --- | --- | --- |
| [同一颗球，三种重力](https://tashan.dev/index.html#cloud/local-437e16a6-d574-4488-ba07-fda85d4e1dbf) | `TS-L-437E16A6D5744488` | `gravity-lab.html`，31,852 字节；PNG 封面，1,883,569 字节 | v1 |
| [古诗入画：山居秋暝教学海报 Prompt](https://tashan.dev/index.html#cloud/local-fc37032e-980a-4809-9b00-25705a4c3757) | `TS-L-FC37032E980A4809` | Markdown，9,101 字节；PNG 示例图，2,972,487 字节 | v1 |

制作说明见 [重力实验室](../examples/gravity-lab/README.md) 和 [古诗入画](../examples/poetry-image-prompt/README.md)。项目图片由图像生成工具制作；下面的 DeepSeek 调用生成文字分析与教学建议，不生成这些图片。教学适配说明是设计建议，尚未课堂试教，不能从技术验收推断学习效果。

## DeepSeek 实际调用

五次请求均经真实 Worker 调用 `deepseek-flash` 并返回 HTTP 200，合计 **11,280 tokens**：workers.dev 四次共 9,715，正式域名追加一次共 1,565。耗时是接口往返时间，不能作为固定响应承诺。

| 任务 | 输入 tokens | 输出 tokens | 合计 | 耗时 |
| --- | ---: | ---: | ---: | ---: |
| 重力项目上传分析 | 1,259 | 364 | 1,623 | 3.536 秒 |
| 古诗项目上传分析 | 2,623 | 250 | 2,873 | 3.041 秒 |
| 重力项目教学建议 | 1,200 | 488 | 1,688 | 4.476 秒 |
| 古诗 Prompt 优化 | 2,606 | 925 | 3,531 | 5.450 秒 |
| 正式域名上传分析复测 | 1,259 | 306 | 1,565 | 3.316 秒 |

已检查返回结构和结果完整性；教师审核、实际课堂使用及教学效果验证仍需另行开展。调用记录不包含在公开静态包中，本文不记录 API key、密码或会话。

## 完整项目备份

已通过同一项目 API 读取两个示例的全部当前版本与文件，并生成格式 2 便携备份；两份均通过应用导入器校验。

| 本机私有备份 | 文件大小 | 内容 |
| --- | ---: | --- |
| `.local/backups/test-projects/gravity-complete.json` | 2,561,649 字节 | 1 个版本、2 个文件 |
| `.local/backups/test-projects/poetry-complete.json` | 4,000,068 字节 | 1 个版本、2 个文件 |

这些备份不含账号表、密码、登录会话或其他账号资料，也不代替全站灾备。完整恢复仍需 Supabase 数据库、旧 Supabase Storage、新 R2 文件及受保护的环境配置；恢复对象版本回执的边界见 [部署与备份](DEPLOYMENT.md#备份与灾难恢复范围)。本轮未执行全站灾难恢复演练。

## 证据与未完成项

本机私有证据均位于 `.local/`：`cloud-qa-report-e677ab1ef1.json`、`r2-negative-report.json`、`demo-upload-report.json`、`demo-ai-report.json`、`demo-backup-report.json`、`domain-qa-report-2a88d63e48.json`、`domain-demo-report.json`。本文仅摘录验收指标；不复制账号凭据文件，私有原始记录和备份不进入 Git 或静态发布包。

本轮浏览器控制连接不可用，因此界面验证依靠 HTTP、脚本／DOM 事件与 VM、原生 workerd 测试；**未完成本轮浏览器视觉、鼠标操作全流程或手机实机验收**。持续负载测试、教师课前审核与课堂试教仍需单独开展，不由上述通过项推定完成。
